# Copy into /etc/nginx/stream-relay.conf on gala-2, preserving existing maps/upstreams.
# Replace placeholders before use.

# Add inside map "$ssl_preread_server_name:$is_anthropic_mcp_client" $tls_upstream {
#   morgen-mcp-<token>.bluepaladin.ai:1 morgen_mcp_proxy;
#   morgen-mcp-<token>.bluepaladin.ai:0 blocked_mcp_proxy;

# Add near other upstream blocks:
# upstream morgen_mcp_proxy {
#     server 127.0.0.1:8448;
# }

# Then validate/reload:
# nginx -t && systemctl reload nginx
